import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('CipherVault smoke test', (WidgetTester tester) async {
    // Test placeholder - CipherVault usa autenticación biométrica
    // que requiere un dispositivo real para testing
    expect(1 + 1, equals(2));
  });
}
